let random = Math.random() * 100
let randomAsInteger = Math.round(random);
console.log(randomAsInteger)

let random1 = Math.random() * 100;
let randomAsInteger1 = Math.round(random);
console.log(randomint);

if (randomAsInteger < randomAsInteger1 && randomAsInteger < 50)
{
    console.log("Zahl 1 ist kleiner als Zahl 2 und Mini");
};

if (randomAsInteger < 30 || randomAsInteger1 < 30)
{
    console.log("Eine der beiden ist kleiner als 30");
};

if (randomAsInteger < 50 && randomAsInteger != 50)
{
    console.log("Erste Zahl klein, zweite kein 50iger");
};
